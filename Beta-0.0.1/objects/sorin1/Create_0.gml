window_set_size(800,500)

vspd = 0;       // velocidade vertical
grv = 0.5;      // gravidade
pulo = -10;     // força do pulo (valor negativo porque vai pra cima)
spd = 2;        // velocidade de andar